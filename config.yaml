http:
  port: "8080"

database:
  host: "localhost"
  port: "5432"
  user: "order_user"
  password: "order_password"
  dbname: "order_service"
  sslmode: "disable"

kafka:
  brokers:
    - "localhost:9092"
  topic: "orders"
  group_id: "order-service-group"